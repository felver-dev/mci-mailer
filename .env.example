# Server
PORT=2525
APP_ENV=development

# Database (PostgreSQL)
DATABASE_URL=postgres://user:password@localhost:5432/mci_mailer?sslmode=disable

# SMTP
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=your-email@example.com
SMTP_FROM_NAME=MCI CARE CI
SMTP_USE_TLS=true
